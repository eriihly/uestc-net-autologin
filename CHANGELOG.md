# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [1.0.2] - 2026-09-19

### 修复

- 修复网关参数提取时引用未定义变量 `joined` 导致的认证失败（`--login` 自动认证必现）。

### 改进

- 开机「等待校园网就绪」最长时长由 90 秒缩短为 **15 秒**，减少网络异常时的无效等待。

## [1.0.1] - 2026-09-18

### 改进

- **开机自启改为快捷方式启动**：不再使用批处理（旧版会在开机时闪现 cmd 窗口），
  改为在启动文件夹创建 `.lnk` 快捷方式直接拉起 `pythonw.exe`，全程无任何窗口。
  开启自启时自动清理旧的 `.bat` 启动项。
- **认证鲁棒性**：网关跳转链中未取到 `sessionId` 时，先复查实际联网状态
  （设备已在线的情况直接判定成功），并自动重试探测，避免开机网络未稳时误报失败。

## [1.0.0] - 2026-09-18

首个公开版本。

### 功能

- **纯 HTTP 秒级认证**（约 1~2 秒）：完整复刻锐捷 SAM + CAS-SSO 认证流程
  - 网关重定向链参数自动提取（sessionId / userIp / nasIp / customPageId）
  - AES-128-ECB 本地加密密码（与登录页 JS 行为完全一致）
  - 自动推进门户流程并确认会话上线（workFlow → userOnline）
- **单文件应用**（`campus_net.py`）：网页控制台 + 命令行模式合一
  - `python campus_net.py` 打开网页控制台（依赖懒加载，未装依赖也能先启动）
  - `python campus_net.py --login` 无界面直接认证（供开机自启）
  - `python campus_net.py --force` 强制完整认证一次（排障）
- **控制台功能**
  - 运行环境自检 + 一键配置依赖（默认使用清华镜像）
  - 账号配置（保存到本机 `config.json`）
  - 开机自动连接开关（一键创建/删除 Windows 启动项）
  - 连接网络 / 强制认证测试（执行输出实时回显）
  - 服务仅监听 `127.0.0.1` 并校验 Host 头（防 DNS 重绑定）
- **开机场景适配**：等待网络就绪、失败自动重试；开机认证静默运行（无窗口，日志写入 `login.log`）
- **Windows 一键脚本**：`start.bat`（打开控制台）

### 安全

- 账号密码仅保存在本机 `config.json`，该文件已加入 `.gitignore`，不会进入版本库
- 控制台网页服务不对局域网开放

### 说明

- 本项目针对作者所在学校的认证系统开发（认证服务器 `110.184.24.61`）。
  同体系（锐捷 SAM + CAS-SSO + ePortal）的其他学校可在控制台的「高级设置」中修改服务器地址后使用；
  不同认证系统需要自行适配代码。
